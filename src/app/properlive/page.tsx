import Home from "@/components/properlive";
import React from "react";

const Properlive = () => {
  return (
    <div className="flex h-full w-screen flex-col items-center justify-center bg-primary-background px-3 py-7 md:px-10">
      <Home />
    </div>
  );
};

export default Properlive;
